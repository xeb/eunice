# Web Search Task

Use the Skill tool to find the web_search skill, then use the provided search.py script to search for the latest news about artificial intelligence developments.

Try both DuckDuckGo (no API key) and Gemini with Google grounding (if GEMINI_API_KEY is set) for comparison.
