{
  "_comment": "MCP servers for eunice sysadmin mode - all verified working as of Nov 2025",
  "mcpServers": {
    "shell": {
      "_comment": "Execute shell commands",
      "command": "uvx",
      "args": [
        "git+https://github.com/emsi/mcp-server-shell"
      ]
    },
    "filesystem": {
      "_comment": "File operations: read, write, list, create, move",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "."
      ]
    },
    "text-editor": {
      "_comment": "Line-oriented text file editing with patch operations and conflict detection",
      "command": "uvx",
      "args": [
        "mcp-text-editor"
      ]
    },
    "grep": {
      "_comment": "Fast code search using ripgrep",
      "command": "npx",
      "args": [
        "-y",
        "mcp-ripgrep@latest"
      ]
    },
    "memory": {
      "_comment": "Persistent memory for context across sessions",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-memory",
        "~/.eunice"
      ]
    },
    "web": {
      "_comment": "Web search via Brave API - REQUIRES BRAVE_API_KEY env var",
      "_setup": "Get free API key at https://brave.com/search/api/ (2,000 queries/month free)",
      "command": "npx",
      "args": [
        "-y",
        "@brave/brave-search-mcp-server"
      ]
    },
    "fetch": {
      "_comment": "HTTP requests and web fetching",
      "command": "uvx",
      "args": [
        "mcp-server-fetch"
      ]
    }
  }
}
